#include <bits/stdc++.h>
using namespace std;

int getOptimal(int arr[], int n)
{
     int res=0;
     // while (All items are not considered)
     // {
     //      i=selectAnItem()
     //      if (feasible(i))
     //           res=res+i;
     // }

     return res;
}

int main()
{
     int arr[]={};
     int n=sizeof(arr)/sizeof(arr[0]);

     int res=getOptimal(arr,n);

     return 0;
}